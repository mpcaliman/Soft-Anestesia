node_modules
package-lock.json
test-results/
playwright-report/

# Seed gerado da base Anvisa/CMED (12,8 MB). Não vai para o repositório: é
# grande demais para colar no editor SQL do Supabase (que é o único motivo de
# ele existir em SQL), e sai de um comando —
#   node scripts/gerar-base-medicamentos.mjs <arquivo-da-cmed>
# O caminho normal de carga é scripts/import-anvisa-medications.mjs.
database/seeds/medicamentos_anvisa.sql

# Arquivo único gerado da base Anvisa/CMED (5,4 MB). Não vai para o repositório
# pelo mesmo motivo do seed: sai de um comando —
#   node scripts/gerar-sql-unico.mjs <arquivo-da-cmed>
database/medicamentos_anvisa_completo.sql
database/partes/
database/partes-celular/
